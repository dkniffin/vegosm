{
  "name": "vegosm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js",
    "build:dev": "esbuild src/js/index.js --bundle --sourcemap --watch --outdir=dist --loader:.png=file --loader:.svg=file --public-path=/dist",
    "serve": "http-server --cors",
    "dev": "npm run serve & npm run build:dev"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.25.9",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "leaflet": "^1.9.4",
    "leaflet-tag-filter-button": "^0.0.5",
    "leaflet.awesome-markers": "^2.0.5",
    "leaflet.locatecontrol": "^0.84.2",
    "opening_hours": "^3.9.0"
  }
}
